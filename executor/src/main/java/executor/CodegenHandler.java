package executor;

import org.json.*;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class CodegenHandler {

    Logger logger = LoggerFactory.getLogger(CodegenHandler.class);

    public String genGroovy(JSONObject flowchart) {
        return "";
    }
}
